{% extends 'base.html.twig' %}

{% block body %}
    <h1>NatureMatrice</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ natureMatrice.id }}</td>
            </tr>
            <tr>
                <th>Valeurmatrice</th>
                <td>{{ natureMatrice.valeurMatrice }}</td>
            </tr>
            <tr>
                <th>Precisionmatrice</th>
                <td>{{ natureMatrice.precisionMatrice }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('naturematrice_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('naturematrice_edit', { 'id': natureMatrice.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
