{% extends 'base.html.twig' %}

{% block body %}
    <h1>MicroOrganisme</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ microOrganisme.id }}</td>
            </tr>
            <tr>
                <th>Nommicroorganisme</th>
                <td>{{ microOrganisme.nomMicroOrganisme }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('microorganisme_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('microorganisme_edit', { 'id': microOrganisme.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
