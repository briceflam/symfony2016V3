{% extends 'base.html.twig' %}

{% block body %}
    <h1>FichePrescription</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fichePrescription.id }}</td>
            </tr>
            <tr>
                <th>Nomficheprescription</th>
                <td>{{ fichePrescription.nomFichePrescription }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ fichePrescription.url }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('ficheprescription_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('ficheprescription_edit', { 'id': fichePrescription.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
