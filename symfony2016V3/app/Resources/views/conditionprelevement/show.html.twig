{% extends 'base.html.twig' %}

{% block body %}
    <h1>ConditionPrelevement</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ conditionPrelevement.id }}</td>
            </tr>
            <tr>
                <th>Valeurcp</th>
                <td>{{ conditionPrelevement.valeurCP }}</td>
            </tr>
            <tr>
                <th>Precisioncp</th>
                <td>{{ conditionPrelevement.precisionCP }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('conditionprelevement_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('conditionprelevement_edit', { 'id': conditionPrelevement.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
